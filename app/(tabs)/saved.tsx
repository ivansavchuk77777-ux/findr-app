import {SafeAreaView,ScrollView,Text,StyleSheet} from 'react-native'; import {Brand,C,Card,Title} from '../../components/UI'; import {Store} from '../../lib/store';
export default function Saved(){return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.page}><Brand/><Title>Saved Finds</Title><Text style={s.sub}>Things you want to come back to.</Text>{Store.saved.length?Store.saved.map(x=><Card key={x.id}><Text style={s.item}>♡ {x.query}</Text></Card>):<Card><Text style={s.item}>No saved finds yet.</Text></Card>}</ScrollView></SafeAreaView>};const s=StyleSheet.create({safe:{flex:1,backgroundColor:C.bg},page:{padding:22,gap:10},sub:{color:C.muted,fontSize:16},item:{color:'#fff',fontSize:18,fontWeight:'800'}})
